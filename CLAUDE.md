# CLAUDE.md — 專案規約

霧湖釣手（Pixel Fishing）：純前端像素風釣魚博弈遊戲。無建置流程、無外部依賴、無框架。

## 第一件事：讀 WIKI

**動任何程式碼之前，先讀 [`wiki/README.md`](wiki/README.md)。**
那是這個專案的知識庫入口，裡面有完整的機制說明、資料規格、程式碼地圖與已知地雷。
不要靠讀原始碼從頭推理——wiki 已經把該知道的整理好了，讀它比讀 1000 行 JS 快。

要改某個東西不知道從哪下手，查 [`wiki/09-recipes.md`](wiki/09-recipes.md)（操作手冊）。
要改數值，查 [`wiki/10-balance-tuning.md`](wiki/10-balance-tuning.md)。
遇到怪現象先查 [`wiki/11-invariants-and-gotchas.md`](wiki/11-invariants-and-gotchas.md)，那裡列了已經踩過的坑。

## 強制規則：所有修改必須回寫 WIKI

**這是硬性規定，不是建議。** 任何一次程式碼修改，在回報完成之前必須同步更新知識庫：

1. **查對照表** — 打開 [`wiki/_map.md`](wiki/_map.md)，找出你改的原始碼檔案對應哪些 wiki 頁面。
2. **更新該頁面** — 內容改變就改內文；新增機制就加小節；刪除功能就刪掉相關段落，不要留孤兒描述。
3. **寫 CHANGELOG** — 在 [`wiki/CHANGELOG.md`](wiki/CHANGELOG.md) 最上方新增一筆，格式見該檔開頭。
4. **維護對照表** — 新增了原始碼檔案，就在 `_map.md` 補一列。
5. **回報時說明** — 回覆使用者時要明確講「已更新 wiki 的哪幾頁」。

### 什麼情況可以不更新 wiki

只有這些：純排版／錯字修正、註解調整、不影響行為的變數改名。
**只要行為、數值、資料結構、檔案結構有任何變動，一律要更新。**

### 更新品質要求

- wiki 是寫給「下一個接手的人或 agent」看的，不是寫給自己備忘。假設讀者不知道任何脈絡。
- 描述機制要寫**為什麼這樣設計**，不只是寫做了什麼。設計取捨是最容易失傳的資訊。
- 引用程式碼用「檔名 › 函式名」（例：`state.js › rollCatch()`），**不要寫行號**，行號會失效。
- 數值改動要同步更新 `10-balance-tuning.md` 裡的平衡表，必要時重跑模擬（該頁附腳本）。
- 踩到新的坑，一定要寫進 `11-invariants-and-gotchas.md`。這頁的價值最高。

## 開發環境

```bash
python -m http.server 5177
```

改完 JS 若畫面沒更新，**強制重新整理（Ctrl+F5）**。`http.server` 會讓瀏覽器快取住舊 JS，這個坑踩過不只一次。

## 程式碼風格

- 原生 JS、傳統 `<script>`、IIFE 包起來掛到 `window.FG`。**不要引入 ES module、打包工具、框架或 npm 套件**，這個專案的價值之一就是雙擊 `index.html` 就能跑。
- 註解用繁體中文，寫在「為什麼」而不是「做什麼」。
- 新增畫面分頁要遵守 `wiki/01-architecture.md` 裡的畫面模組契約。
